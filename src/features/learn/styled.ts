import styled from 'styled-components'

import { Button } from 'common/components'

export const TitleCardPack = styled.p`
  font-weight: 600;
  font-size: 22px;
  line-height: 27px;
  color: #000;
  text-align: center;
  margin-bottom: 30px;
`
export const TextTitle = styled.p`
  font-weight: 600;
  font-size: 16px;
  line-height: 24px;
  color: #000;
  margin-bottom: 13px;
`
export const TextSpan = styled.span`
  font-weight: 400;
  font-size: 16px;
  line-height: 24px;
  color: #000;
`

export const DescribeShot = styled.p`
  font-weight: 400;
  line-height: 24px;
  font-size: 14px;
  color: #000;
`

export const ButtonLearn = styled(Button)`
  margin-top: 30px;
`
